<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des Utilisateurs</title>
    <link rel="stylesheet" href="/styleadministration.css">
</head>
<body>
    <header>
        <div class="principale">
            <div class="logo">
                <a href="#"><img src="/images/logo.png" alt="USLife Logo"></a>
            </div>
            <nav>
                <ul>
                    <li><a href="/owner-home">Accueil</a></li>
                    <li><a href="#">Service</a></li>
                    <li><a href="#">Blog</a></li>
                    <li><a href="#">À propos de nous</a></li>
                    <li><a href="#">Contact</a></li>
                    <li><a href="/owner/gestionowner">Gestion</a></li>
                    <li class="active"><a href="/owner/administration">Administration</a></li>
                </ul>
            </nav>
        </div>
    </header>
    <main>
        <div class="container">
            <h1>Gestion des Utilisateurs</h1>
            <table>
                <thead>
                    <tr>
                        <th>Nom d'utilisateur</th>
                        <th>ID Discord</th>
                        <th>Rôle</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(user => { %>
                        <tr>
                            <td><%= user.username %></td>
                            <td><%= user.discordId %></td>
                            <td><%= user.role %></td>
                            <td>
                                <% if (user.role !== 'owner') { %>
                                    <% if (user.role === 'admin') { %>
                                        <form action="/owner/remove-admin/<%= user._id %>" method="post" class="action-form">
                                            <button type="submit" class="btn">Retirer Admin</button>
                                        </form>
                                    <% } else { %>
                                        <form action="/owner/make-admin/<%= user._id %>" method="post" class="action-form">
                                            <button type="submit" class="btn">Nommer Admin</button>
                                        </form>
                                    <% } %>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </main>
    <footer class="footer">
        <p>Un serveur créé par la communauté pour la communauté !</p>
        <div class="social-links">
            <a href="https://discord.com/invite/uslife"><i class="fab fa-discord"></i> Discord</a>
            <a href="https://www.instagram.com/uslifegram/"><i class="fab fa-instagram"></i> Instagram</a>
            <a href="https://www.youtube.com/channel/UCxaKqZu_dMZKETYwInHy6WA"><i class="fab fa-youtube"></i> YouTube</a>
        </div>
        <p>Copyright © 2024 USLife - Tous droits réservés.</p>
    </footer>
</body>
</html>
